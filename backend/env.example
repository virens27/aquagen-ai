# Copy this file to .env and fill in real values.
# .env must stay in .gitignore — never commit real credentials.

# --- Supabase Postgres ---
# Local dev: either connection type works.
# Once deployed to Render: use the Session Pooler URI (IPv4-compatible).
# Format: postgresql+psycopg://postgres:<password>@<host>:5432/postgres
DATABASE_URL=postgresql+psycopg://postgres:virenshende2782@db.kiooihqrshamtxzaqqyx.supabase.co:5432/postgres

# --- App ---
APP_ENV=development
DEBUG=true

# --- LLM ---
GROQ_API_KEY=

# --- CORS ---
FRONTEND_ORIGIN=http://localhost:5173
